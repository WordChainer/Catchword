version: "2"
checks:
  argument-count:
    config:
      threshold: 5
  method-complexity:
    config:
      threshold: 10
  method-lines:
    config:
      threshold: 80
  similar-code:
    enabled: false  
  identical-code:
    enabled: false
